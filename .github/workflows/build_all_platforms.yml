name: Build Octo Installers (Windows + Linux)

on:
  workflow_dispatch:
    inputs:
      platform:
        description: 'Welche Plattform bauen? windows/linux/all'
        required: false
        default: 'all'

env:
  OCTO_TOKEN: token1
  OCTO_SERVER: http://127.0.0.1:5000

jobs:
  build-windows:
    if: ${{ github.event.inputs.platform == 'all' || github.event.inputs.platform == 'windows' }}
    name: Build Windows Installers
    runs-on: [self-hosted, windows, x64, octo]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Remove old Windows ZIP
        shell: powershell
        run: |
          if (Test-Path "octo-windows.zip") { Remove-Item "octo-windows.zip" -Force }

      - name: Build all Windows components
        shell: powershell
        run: |
          $components = @("client","server","runner")
          $rootPath = $PWD
          foreach ($comp in $components) {
              Write-Host "=============================="
              Write-Host "Building $comp for Windows"
              Write-Host "=============================="

              $path = Join-Path $rootPath $comp
              if (-not (Test-Path $path)) { Write-Error "$path not found!"; exit 1 }
              Set-Location $path

              if (-not (Test-Path "build_installer.ps1")) { Write-Error "build_installer.ps1 not found"; exit 1 }

              Write-Host "Running build_installer.ps1 for $comp..."
              & ".\build_installer.ps1"
              if ($LASTEXITCODE -ne 0) { Write-Error "$comp build failed!"; exit 1 }

              Set-Location $rootPath
          }

      - name: Package Windows installers
        shell: powershell
        run: |
          Write-Host "Current directory: $PWD"
          Write-Host "Listing all files in workspace:"
          Get-ChildItem -Recurse | ForEach-Object { Write-Host $_.FullName }
          
          $components = @("client","server","runner")
          $installerFiles = @()
          
          foreach ($comp in $components) {
              Write-Host "Looking for installer for component: $comp"
              # Join paths in two steps
              $compPath = Join-Path $PWD $comp
              $installer = Join-Path $compPath "octo-$comp-0.1-setup.exe"
              Write-Host "Checking path: $installer"
              
              if (Test-Path $installer) {
                  Write-Host "Found installer at: $installer"
                  $installerFiles += $installer
              } else {
                  Write-Error "Expected installer not found: $installer"
                  exit 1
              }
          }

          Write-Host "Found installer files:"
          $installerFiles | ForEach-Object { Write-Host "- $_" }

          Compress-Archive -Path $installerFiles -DestinationPath "octo-windows.zip" -Force
          Write-Host "Windows ZIP created with exact installer files."

      - name: Upload Windows ZIP
        uses: actions/upload-artifact@v4
        with:
          name: octo-windows
          path: octo-windows.zip
          retention-days: 30

  build-linux:
    if: ${{ github.event.inputs.platform == 'all' || github.event.inputs.platform == 'linux' }}
    name: Build Linux Installers
    runs-on: [self-hosted, windows, x64, octo]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Remove old Linux ZIP
        shell: powershell
        run: |
          if (Test-Path "octo-linux.zip") { Remove-Item "octo-linux.zip" -Force }

      - name: Octo login
        shell: powershell
        run: |
          chcp 65001 > $null
          octo login --token $env:OCTO_TOKEN --server $env:OCTO_SERVER

      - name: Configure Linux Docker
        shell: powershell
        run: octo config --docker python:3.11

      - name: Build all Linux components
        shell: powershell
        run: |
          $components = @("client","server","runner")
          $rootPath = $PWD
          foreach ($comp in $components) {
            Write-Host "=============================="
            Write-Host "Building $comp for Linux"
            Write-Host "=============================="

            $path = Join-Path $rootPath $comp
            if (-not (Test-Path $path)) { Write-Error "$path not found!"; exit 1 }
            Set-Location $path

            if (-not (Test-Path "start_build.sh")) { Write-Error "start_build.sh not found"; exit 1 }

            Write-Host "Running start_build.sh for $comp..."
            # Run chmod and execute in a single command through octo
            octo run ./start_build.sh
            if ($LASTEXITCODE -ne 0) { Write-Error "$comp build failed!"; exit 1 }

            Set-Location $rootPath
          }

      - name: Package Linux installers
        shell: powershell
        run: |
          Write-Host "Current directory: $PWD"
          Write-Host "Listing all files in workspace:"
          Get-ChildItem -Recurse | ForEach-Object { Write-Host $_.FullName }
          
          $components = @("client","server","runner")
          $installerFiles = @()
          
          foreach ($comp in $components) {
              Write-Host "Looking for .deb package for component: $comp"
              # Join paths in two steps
              $compPath = Join-Path $PWD $comp
              $installer = Join-Path $compPath "octo-$comp`_0.1_amd64.deb"
              Write-Host "Checking path: $installer"
              
              if (Test-Path $installer) {
                  Write-Host "Found .deb package at: $installer"
                  $installerFiles += $installer
              } else {
                  Write-Error "Expected .deb package not found: $installer"
                  exit 1
              }
          }

          Write-Host "Found .deb packages:"
          $installerFiles | ForEach-Object { Write-Host "- $_" }

          Compress-Archive -Path $installerFiles -DestinationPath "octo-linux.zip" -Force
          Write-Host "Linux ZIP created with exact .deb files."

      - name: Upload Linux ZIP
        uses: actions/upload-artifact@v4
        with:
          name: octo-linux
          path: octo-linux.zip
          retention-days: 30
