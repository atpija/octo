name: Build Octo Installers (Windows + Linux)

on:
  workflow_dispatch:
    inputs:
      platform:
        description: 'Welche Plattform bauen?'
        required: true
        type: choice
        default: 'all'
        options:
          - all
          - windows
          - linux

env:
  OCTO_TOKEN: demo-token
  OCTO_SERVER: http://127.0.0.1:5000

# ============================================================
# WINDOWS BUILD
# ============================================================
jobs:
  build-windows:
    if: ${{ github.event.inputs.platform == 'all' || github.event.inputs.platform == 'windows' }}
    name: Build Windows Installers
    runs-on: [self-hosted, windows, x64, octo]
    env:
      LOCAL_EXE_CACHE: C:\octo\artefacts\${{ github.run_id }}\windows
    steps:
      - uses: actions/checkout@v4
      
      - name: Build Windows components
        shell: powershell
        run: |
          $components = @("client","server","runner")
          foreach ($c in $components) {
            cd $c
            ./build_installer.ps1
            if ($LASTEXITCODE -ne 0) { exit 1 }
            cd ..
          }
      
      - name: Collect .exe files into windows_exes/
        shell: powershell
        run: |
          Write-Host "Collecting .exe files..."
          if (Test-Path windows_exes) {
            Remove-Item windows_exes -Recurse -Force
          }
          New-Item -ItemType Directory -Force -Path windows_exes | Out-Null
          $exeFiles = Get-ChildItem -Recurse -Filter *.exe |
            Where-Object { $_.FullName -notmatch '\\windows_exes\\' }
          if (-not $exeFiles) {
            Write-Error "No .exe files found!"
            exit 1
          }
          foreach ($file in $exeFiles) {
            Copy-Item $file.FullName windows_exes -Force
          }
          Get-ChildItem windows_exes
      
      # ------------------------------------------------------------
      # LOCAL ARTEFACT CACHE (NEW)
      # ------------------------------------------------------------
      - name: Cache .exe files locally
        shell: powershell
        run: |
          Write-Host "Caching exes to $env:LOCAL_EXE_CACHE"
          if (Test-Path $env:LOCAL_EXE_CACHE) {
            Remove-Item $env:LOCAL_EXE_CACHE -Recurse -Force
          }
          New-Item -ItemType Directory -Force -Path $env:LOCAL_EXE_CACHE | Out-Null
          Copy-Item windows_exes\*.exe $env:LOCAL_EXE_CACHE -Force
          Write-Host "Cached exes:"
          Get-ChildItem $env:LOCAL_EXE_CACHE
      
      # -------------------------------------------------
      # Upload Artefacts (7 Tage)
      # -------------------------------------------------
      - name: Upload Octo Windows packages
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: octo-windows-packages
          retention-days: 7
          path: windows_exes/

# ============================================================
# LINUX BUILD (Octo parallel)
# ============================================================
  build-linux:
    if: ${{ github.event.inputs.platform == 'all' || github.event.inputs.platform == 'linux' }}
    name: Build Linux Installers
    runs-on: [self-hosted, windows, x64, octo]
    env:
      LOCAL_DEB_CACHE: C:\octo\artefacts\${{ github.run_id }}\linux
    steps:
      - uses: actions/checkout@v4
      
      - name: Octo login
        shell: powershell
        run: |
          octo login --token $env:OCTO_TOKEN --server $env:OCTO_SERVER
          octo config --docker octo_build_env:latest
      
      - name: Build Linux packages in parallel
        shell: powershell
        run: |
          $root = Get-Location
          $jobs = @()
          foreach ($c in @("client","server","runner")) {
            $jobs += Start-Process powershell `
              -ArgumentList "-Command cd `"$root/$c`"; octo run ./start_build.sh" `
              -PassThru
          }
          $jobs | Wait-Process
      
      - name: Collect .deb files into docker_debs/
        shell: powershell
        run: |
          Write-Host "Collecting .deb files..."
          if (Test-Path docker_debs) {
            Remove-Item docker_debs -Recurse -Force
          }
          New-Item -ItemType Directory -Force -Path docker_debs | Out-Null
          $debFiles = Get-ChildItem -Recurse -Filter *.deb |
            Where-Object { $_.FullName -notmatch '\\docker_debs\\' }
          if (-not $debFiles) {
            Write-Error "No .deb files found!"
            exit 1
          }
          foreach ($file in $debFiles) {
            Copy-Item $file.FullName docker_debs -Force
          }
          Get-ChildItem docker_debs
      
      # ------------------------------------------------------------
      # LOCAL ARTEFACT CACHE (NEW)
      # ------------------------------------------------------------
      - name: Cache .deb files locally
        shell: powershell
        run: |
          Write-Host "Caching debs to $env:LOCAL_DEB_CACHE"
          if (Test-Path $env:LOCAL_DEB_CACHE) {
            Remove-Item $env:LOCAL_DEB_CACHE -Recurse -Force
          }
          New-Item -ItemType Directory -Force -Path $env:LOCAL_DEB_CACHE | Out-Null
          Copy-Item docker_debs\*.deb $env:LOCAL_DEB_CACHE -Force
          Write-Host "Cached debs:"
          Get-ChildItem $env:LOCAL_DEB_CACHE
      
      # -------------------------------------------------
      # Upload Artefacts (7 Tage)
      # -------------------------------------------------
      - name: Upload Octo Linux packages
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: octo-linux-packages
          retention-days: 7
          path: docker_debs/

# ============================================================
# DOCKER IMAGE BUILD (INFRA + TEST BASE PARALLEL)
# ============================================================
  build-docker:
    name: Build Docker Images
    uses: ./.github/workflows/build_docker.yml
    needs: build-linux
    if: ${{ github.event.inputs.platform == 'all' || github.event.inputs.platform == 'linux' }}

# ============================================================
# SETUP WSL INSTALL NEW OCTO RUNNER + SERVER
# ============================================================
  wsl-infra-up:
    name: Install Octo Server + Runner in WSL
    needs: build-linux
    runs-on: [self-hosted, windows, x64, octo]

    env:
      DEB_DIR_WIN: C:\octo\artefacts\${{ github.run_id }}

    steps:
      - uses: actions/checkout@v4

      - name: Install Octo in WSL
        shell: powershell
        run: |
          $deb = $env:DEB_DIR_WIN -replace '\\','/'
          $deb = $deb -replace '^C:', '/mnt/c'
          Write-Host "WSL deb dir: $deb"

          wsl bash scripts/install-wsl-octo.sh "$deb"

      - name: Verify Octo installation in WSL
        shell: powershell
        run: |
          wsl bash -lc "echo 'WSL OK'; which octo-server; which octo-runner; octo-server --help"

# ============================================================
# Test Set (PARALLEL, OCTO ORCHESTRATED)
# ============================================================
  testset:
    name: Run Octo Test Set (parallel)
    runs-on: [self-hosted, windows, x64, octo]
    needs:
      - wsl-infra-up
      - build-docker
    if: ${{ github.event.inputs.platform == 'all' || github.event.inputs.platform == 'linux' }}

    steps:
      - uses: actions/checkout@v4

      - name: Configure Octo
        shell: powershell
        run: |
          octo login --token $env:OCTO_TOKEN --server $env:OCTO_SERVER
          octo config --docker octo-test-base

      - name: Run tests in parallel via Octo
        shell: powershell
        run: |
          # Funktion zum Parsen von JUnit XML
          function Get-TestCasesFromJUnit {
              param([string]$xmlPath)
              
              if (-not (Test-Path $xmlPath)) {
                  return @()
              }
              
              try {
                  [xml]$xml = Get-Content $xmlPath -Raw -Encoding UTF8
                  $cases = @()
                  
                  foreach ($testcase in $xml.testsuites.testsuite.testcase) {
                      $cases += [PSCustomObject]@{
                          name    = $testcase.name
                          time    = $testcase.time
                          failure = $testcase.failure
                          skipped = $testcase.skipped
                      }
                  }
                  
                  return $cases
              }
              catch {
                  Write-Warning "Could not parse JUnit XML: $xmlPath"
                  return @()
              }
          }
          
          # Hauptlogik
          $root = Get-Location
          $tests = @(
            "smoketest.py",
            "test_daily.py",
            "test_file_upload.py",
            "test_docker.py",
            "test_install_feat.py"
          )
          $jobs = @()
          $exitCodeFiles = @()
          New-Item -ItemType Directory -Force test-reports | Out-Null
          
          foreach ($test in $tests) {
            $exitFile = "$env:TEMP\exit-$test.txt"
            $exitCodeFiles += $exitFile
            $script = @"
          cd '$root/test'
          octo run $test
          `$LASTEXITCODE | Out-File '$exitFile' -NoNewline
          exit `$LASTEXITCODE
          "@
            $jobs += Start-Process powershell `
              -ArgumentList "-Command", $script `
              -NoNewWindow `
              -PassThru
          }
          $jobs | Wait-Process
          
          # GitHub Job Summary
          $summary = "## Octo Test Summary`n`n"
          $summary += "| Testfile | Status |`n"
          $summary += "|----------|--------|`n"
          
          for ($i = 0; $i -lt $tests.Count; $i++) {
              $test = $tests[$i]
              $code = [int](Get-Content $exitCodeFiles[$i] -Raw)
              $name = [System.IO.Path]::GetFileNameWithoutExtension($test)
              $xmlPath = "test-reports/junit-$name.xml"
              
              if ($code -eq 0) {
                  $summary += "| $test | :white_check_mark: PASS |`n"
              } else {
                  $summary += "| $test | :x: FAIL |`n"
              }
              
              # Einzelne Testfälle (Details)
              $cases = Get-TestCasesFromJUnit $xmlPath
              if ($cases.Count -gt 0) {
                  $summary += "`n<details>`n"
                  $summary += "<summary>Testcase Overview</summary>`n`n"
                  $summary += "| Testcase | Status | Duration (s) |`n"
                  $summary += "|----------|--------|--------------|`n"
                  
                  foreach ($case in $cases) {
                      $status = ":white_check_mark: PASS"
                      if ($case.failure) { $status = ":x: FAIL" }
                      elseif ($case.skipped) { $status = ":fast_forward: SKIP" }
                      $summary += "| $($case.name) | $status | $($case.time) |`n"
                  }
                  $summary += "`n</details>`n"
              }
          }
          
          $summary | Out-File `
            -FilePath $env:GITHUB_STEP_SUMMARY `
            -Encoding utf8 `
            -Append

      # -------------------------------------------------
      # Upload Reports (7 Tage)
      # -------------------------------------------------
      - name: Upload pytest reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pytest-reports
          retention-days: 7
          path: |
            test/test-reports/*.xml
            test/test-reports/*.html

# ============================================================
# CLEANUP
# ============================================================
  cleanup:
    name: Cleanup
    runs-on: [self-hosted, windows, x64, octo]
    needs:
      - build-windows
      - build-linux
      - testset
    if: always()

    steps:
      - name: Cleanup old runner cache
        shell: powershell
        run: |
          $base = "C:\octo\runner-cache\linux"
          if (Test-Path $base) {
            Get-ChildItem $base -Directory |
              Where-Object { $_.LastWriteTime -lt (Get-Date).AddDays(-14) } |
              Remove-Item -Recurse -Force
          }
